{
  "name": "webguard",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "packages/*",
    "security-lab"
  ],
  "scripts": {
    "build:packages": "npm run build --workspace=@webguard/shared && npm run build --workspace=@webguard/scanner",
    "dev": "npm run build:packages && concurrently --kill-others-on-fail --names \"API,WEB,LAB\" --prefix-colors \"cyan,magenta,yellow\" \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\" \"npm run dev --workspace=security-lab\"",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
